---
name: Build ISO

# Trigger options:
#   1. Manually via Actions → "Build ISO" → "Run workflow" (any branch).
#   2. By commenting '/build-iso' on a pull request (repo owners and members only).
#      The ISO is built from the PR's head branch.
on:
  workflow_dispatch:
  issue_comment:
    types: [created]

# Restrict default permissions (least privilege)
permissions:
  contents: read

concurrency:
  group: build-iso-${{ github.event.issue.number || github.run_id }}
  cancel-in-progress: true

env:
  IMAGE_NAME: "${{ github.event.repository.name }}"

jobs:
  build-iso:
    name: Build and upload ISO
    runs-on: ubuntu-24.04
    timeout-minutes: 90

    # Run on:
    #   • manual dispatch, OR
    #   • a '/build-iso' comment posted on a PR by OWNER or MEMBER only
    #     (COLLABORATOR removed for tighter security — only org members can trigger)
    if: >-
      github.event_name == 'workflow_dispatch' || (
        github.event_name == 'issue_comment' &&
        github.event.issue.pull_request != null &&
        contains(github.event.comment.body, '/build-iso') &&
        contains(fromJson('["OWNER","MEMBER"]'), github.event.comment.author_association)
      )

    permissions:
      contents: write  # needed to create/update GitHub Releases
      packages: read
      pull-requests: write  # needed to post the 👍 reaction

    steps:
      # Acknowledge the slash command so the author knows the build started
      - name: React to comment
        if: github.event_name == 'issue_comment'
        run: >-
          gh api
          repos/${{ github.repository }}/issues/comments/${{ github.event.comment.id }}/reactions
          -X POST -f content='+1'
        env:
          GH_TOKEN: ${{ github.token }}

      # Free up disk space – ISOs can exceed 4 GB
      - name: Maximize build space
        uses: ublue-os/remove-unwanted-software@v7

      # When triggered from a PR comment, resolve the PR's head branch name
      - name: Get PR head ref
        if: github.event_name == 'issue_comment'
        id: pr_ref
        run: |
          PR_REF=$(gh pr view ${{ github.event.issue.number }} \
            --repo ${{ github.repository }} \
            --json headRefName --jq '.headRefName')
          echo "ref=${PR_REF}" >> $GITHUB_OUTPUT
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          # Check out the PR branch when triggered via comment; default ref otherwise
          ref: ${{ github.event_name == 'issue_comment' && steps.pr_ref.outputs.ref || github.ref }}

      - name: Prepare environment
        id: vars
        run: |
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> "${GITHUB_ENV}"
          # Generate the short SHA here
          echo "sha_short=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Build container image
        id: build_image
        uses: redhat-actions/buildah-build@v2
        with:
          containerfiles: |
            ./Containerfile
          image: ${{ env.IMAGE_NAME }}
          tags: latest
          oci: false
          build-args: |
            SHA_HEAD_SHORT=${{ steps.vars.outputs.sha_short }}
            BUILD_STAMP=${{ github.run_id }}

      - name: Make image available to root podman storage
        run: |
          IMAGE_REF="localhost/${{ env.IMAGE_NAME }}:latest"

          # bootc-image-builder runs in a privileged container and reads host
          # storage from /var/lib/containers/storage, so ensure root can see it.
          if ! sudo podman image exists "$IMAGE_REF"; then
            if podman image exists "$IMAGE_REF"; then
              ARCHIVE="/tmp/${{ env.IMAGE_NAME }}-latest.ociarchive"
              podman save --format oci-archive -o "$ARCHIVE" "$IMAGE_REF"
              sudo podman load -i "$ARCHIVE"
            else
              echo "Local image $IMAGE_REF not found in user storage." >&2
              echo "Build step may not have produced a local tag as expected." >&2
              exit 1
            fi
          fi

          sudo podman image exists "$IMAGE_REF"

      - name: Build ISO with bootc-image-builder
        run: |
          mkdir -p /tmp/iso-output
          mkdir -p /tmp/iso-config
          cp disk_config/iso.toml /tmp/iso-config/iso.toml
          sudo podman run \
            --rm \
            --privileged \
            --pull=newer \
            --security-opt label=type:unconfined_t \
            -v /var/lib/containers/storage:/var/lib/containers/storage \
            -v /tmp/iso-config:/config:ro \
            -v /tmp/iso-output:/output \
            quay.io/centos-bootc/bootc-image-builder:latest \
            --type iso \
            --rootfs xfs \
            --config /config/iso.toml \
            --output /output \
            localhost/${{ env.IMAGE_NAME }}:latest

      - name: Upload ISO artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v4.6.0
        with:
          name: ${{ env.IMAGE_NAME }}-iso
          path: /tmp/iso-output/
          retention-days: 7
          if-no-files-found: error

      - name: Publish ISO as GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          ISO_FILE=$(find /tmp/iso-output -name '*.iso' | head -n1)
          RELEASE_TAG="iso-$(date -u +'%Y%m%d-%H%M%S')"
          gh release create "${RELEASE_TAG}" \
            --repo "${{ github.repository }}" \
            --title "ISO ${RELEASE_TAG}" \
            --notes "Automated ISO build from commit ${{ github.sha }}." \
            "${ISO_FILE}"
